name: Deploy to Production

on:
  workflow_run:
    workflows: ["Lint and Test"]
    branches: [main]
    types: [completed]

jobs:
  deploy:
    name: Deploy to Production
    if: github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    environment: ci
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PROD_DIR: ${{ vars.DO_PROD_DIR }}
        with:
          host: ${{ secrets.DO_SSH_HOST }}
          username: ${{ secrets.DO_SSH_USER }}
          key: ${{ secrets.DO_SSH_KEY }}
          envs: GITHUB_TOKEN,PROD_DIR
          script: |
            git -C "$PROD_DIR" fetch "https://x-access-token:${GITHUB_TOKEN}@github.com/steezeburger/brainspread.git" main
            git -C "$PROD_DIR" reset --hard FETCH_HEAD
            cd "$PROD_DIR/packages/django-app"
            docker compose -f docker-compose.yml -f docker-compose.prod.yml build web
            just migrate
            docker compose stop web
            docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d web
